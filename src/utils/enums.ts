export enum Role {
  Admin = "1",
  User = "2",
}

export enum TaskStatuses {
  ToDo = "1",
  InProgress = "2",
  Resolved = "3",
  Reopened = "4",
}

export enum TaskTypes {
  Task = "1",
  Bug = "2",
}
